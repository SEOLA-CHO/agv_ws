Core
Drivers
EWARM
Middlewares
.mxproject
Makefile
*.s
*.ld
build
.vscode
libmicroros
custombuild

# This project intentionally versions the prebuilt Humble library and headers
# so a fresh STM32CubeIDE checkout builds without Docker.
!microros_static_library_ide/libmicroros/
!microros_static_library_ide/libmicroros/**
